# Crowd_Detection_STToolkit

This code has:
- (Extra) Uploads every 30 min of the last 30 min via `sendCrowdingData_30min.py`
- All messages via MQTT
- Counts saved in database StoredMeasurements if no connection to server
