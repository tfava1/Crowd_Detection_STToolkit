# Crowd Sensor Legacy

This code has:
- (Extra) Count upload every 30 min of the last 30 min via `sendCrowdingData_30min.py`
- All messages via MQTT
- Counts saved in database `StoredMeasurements.db` if no connection to server
